const Navbar = () => {
  return (
    <div className="bg-white w-full flex flex-col justify-between h-20 pt-4 sm:pt-0 items-center">
      <div className="w-full h-full pe-4 flex flex-col"></div>

      <div className="w-full h-[2px] bg-gradient-to-r from-transparent via-primary to-transparent" />
    </div>
  );
};

export default Navbar;
